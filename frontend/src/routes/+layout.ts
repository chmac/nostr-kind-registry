// This ensures that all pages are pre-rendered, which in turn ensures that they
// are included as separate HTML pages in our static build
export const prerender = true;
export const trailingSlash = 'always';
